document.addEventListener('DOMContentLoaded',()=>{
    const topButton=document.getElementById('top-button');
    if(topButton) topButton.addEventListener('click',()=>{ location.href='title.html'; });
});
